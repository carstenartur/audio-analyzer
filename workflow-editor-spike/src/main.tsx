import React from 'react'
import ReactDOM from 'react-dom/client'
import WorkflowEditorComponent from '../WorkflowEditorComponent'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WorkflowEditorComponent />
  </React.StrictMode>,
)
