/**
 * Dataset descriptors and importer contracts for real-world and synthetic acoustic corpora.
 *
 * <p>This package intentionally stays independent from classifier internals so that one normalized
 * manifest can feed feature extraction, detection and benchmarking pipelines.
 */
package org.hammer.audio.experimental.acoustic.dataset;
