/**
 * Minimal benchmark-facing extension points.
 *
 * <p>These types intentionally stop at neutral comparator and metric contracts so future
 * benchmarking work can reuse the existing scenario model without forcing a concrete evaluation
 * pipeline yet.
 */
package org.hammer.audio.experimental.acoustic.benchmark;
