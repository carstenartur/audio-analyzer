/**
 * Localization benchmark framework for acoustic localization algorithms.
 *
 * <p>Multiple {@link
 * org.hammer.audio.experimental.acoustic.benchmark.localization.LocalizationBenchmark}
 * implementations can be compared on the same set of simulation scenarios. Adding a new
 * localization algorithm requires only implementing the interface.
 *
 * <p>Metrics include mean localization error, tracking error, false-positive count and
 * false-negative count.
 */
package org.hammer.audio.experimental.acoustic.benchmark.localization;
