/**
 * Collaboration/application-service layer around semantic workflow operations.
 *
 * <p>This package adds collaboration modes, actor metadata, scoped undo policies, an event-bus
 * boundary with transactional outbox abstraction, and non-semantic presence state. The canonical
 * workflow graph and edit semantics remain in {@code org.hammer.audio.workflow}.
 */
package org.hammer.audio.workflow.collaboration;
